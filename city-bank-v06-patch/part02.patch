 = [Vector2i(1,2), Vector2i(3,2), Vector2i(5,2), Vector2i(1,4), Vector2i(3,4), Vector2i(5,4), Vector2i(6,5)]
     for stop in stops:
-        var p = Vector2(stop.x * s.x, stop.y * s.y)
+        var p = _road_point(stop.x, stop.y, s) + Vector2(24, -16)
         draw_rect(Rect2(p - Vector2(12,5), Vector2(24,10)), Color("cad3d7"), true)
         draw_rect(Rect2(p - Vector2(10,3), Vector2(20,6)), Color("4e7e98"), true)
         draw_line(p + Vector2(13,-10), p + Vector2(13,8), Color("d8c47b"), 2.0)
         draw_circle(p + Vector2(13,-11), 3.0, Color("e7b94e"))
 
-func _draw_parking_and_plazas(s: Vector2) -> void:
-    var plaza = Rect2(s.x * 0.515, s.y * 0.535, s.x * 0.11, s.y * 0.095)
-    draw_rect(plaza, Color("87867c"), true)
-    for i in range(7):
-        draw_line(Vector2(plaza.position.x + 8, plaza.position.y + 8 + i * 9), Vector2(plaza.end.x - 8, plaza.position.y + 8 + i * 9), Color("aaa99d"), 1.0)
-    draw_circle(plaza.get_center(), 9.0, Color("557a89"))
+func _draw_traffic_signals(s: Vector2) -> void:
+    var green_phase = int(world_time * 80.0) % 2 == 0
+    for ix in [1,3,5]:
+        for iy in [2,4]:
+            var p = _road_point(ix, iy, s) + Vector2(18, -18)
+            draw_rect(Rect2(p - Vector2(3,7), Vector2(6,14)), Color("1c2328"), true)
+            draw_circle(p + Vector2(0,-3.5), 2.0, Color("56b66d") if green_phase else Color("613837"))
+            draw_circle(p + Vector2(0,3.5), 2.0, Color("633833") if green_phase else Color("d85c52"))
 
-    var lot = Rect2(s.x * 0.79, s.y * 0.58, s.x * 0.15, s.y * 0.10)
-    draw_rect(lot, Color("4a5053"), true)
+func _draw_parking_and_plazas(s: Vector2) -> void:
+    var plaza = Rect2(s.x * 0.525, s.y * 0.525, s.x * 0.105, s.y * 0.065)
+    draw_rect(plaza, Color("88877c"), true)
+    draw_circle(plaza.get_center(), 8.0, Color("557a89"))
+    for i in range(5):
+        _draw_tree(plaza.position + Vector2(12 + i * 22, 12 + (i % 2) * 25), 4.0)
+    var lot = Rect2(s.x * 0.775, s.y * 0.625, s.x * 0.16, s.y * 0.065)
+    draw_rect(lot, Color("444b50"), true)
     for i in range(8):
-        var px = lot.position.x + 12 + i * max(13.0, lot.size.x / 9.0)
-        draw_line(Vector2(px, lot.position.y + 5), Vector2(px, lot.end.y - 5), Color("777e82"), 1.0)
+        var px = lot.position.x + 10 + i * max(12.0, lot.size.x / 9.0)
+        draw_line(Vector2(px, lot.position.y + 4), Vector2(px, lot.end.y - 4), Color("777e82"), 1.0)
         if i % 2 == 0:
-            _draw_vehicle(Vector2(px + 5, lot.position.y + 20 + (i % 3) * 16), 14, 6, [Color("a8b5bf"),Color("b85b4f"),Color("477ba2")][i % 3], PI * 0.5)
+            _draw_vehicle(Vector2(px + 4, lot.position.y + 17 + (i % 3) * 12), 13, 5.5, [Color("a8b5bf"),Color("b85b4f"),Color("477ba2")][i % 3], PI * 0.5, false)
 
 func _draw_industry_details(s: Vector2) -> void:
-    var base = Vector2(s.x * 0.82, s.y * 0.61)
+    var base = Vector2(s.x * 0.81, s.y * 0.64)
     for i in range(3):
-        var p = base + Vector2(i * 42, 0)
-        draw_circle(p, 16, Color("8a8d87"))
-        draw_circle(p, 11, Color("5c6668"))
-        draw_line(p + Vector2(0,-16), p + Vector2(0,-32), Color("656b6c"), 5.0)
-    draw_rect(Rect2(s.x*0.78, s.y*0.67, s.x*0.16, 10), Color("9b7048"), true)
+        var p = base + Vector2(i * 38, 0)
+        draw_circle(p, 14, Color("8a8d87"))
+        draw_circle(p, 9, Color("5c6668"))
+        draw_line(p + Vector2(0,-14), p + Vector2(0,-30), Color("656b6c"), 5.0)
+        if i == 1:
+            draw_circle(p + Vector2(2,-40), 8, Color(0.68,0.70,0.69,0.10 + max(0.0, float(economy.get("gdp_growth",2.0))) * 0.015))
 
 func _draw_university_details(s: Vector2) -> void:
-    var field = Rect2(s.x*0.78, s.y*0.275, s.x*0.13, s.y*0.09)
+    var field = Rect2(s.x*0.795, s.y*0.345, s.x*0.115, s.y*0.055)
     draw_rect(field, Color("4d7a52"), true)
-    draw_rect(field.grow(-5), Color("6c966b"), false, 2.0)
-    draw_line(Vector2(field.get_center().x, field.position.y+5), Vector2(field.get_center().x, field.end.y-5), Color("c6d0b9"), 1.0)
-    draw_circle(field.get_center(), 11, Color("c6d0b9"), false, 1.0)
+    draw_rect(field.grow(-4), Color("c6d0b9"), false, 1.2)
+    draw_line(Vector2(field.get_center().x, field.position.y+4), Vector2(field.get_center().x, field.end.y-4), Color("c6d0b9"), 1.0)
+    draw_circle(field.get_center(), 9, Color("c6d0b9"), false, 1.0)
 
 func _draw_construction(s: Vector2) -> void:
     var growth = float(economy.get("gdp_growth", 2.0))
-    if growth < 0.5:
+    if growth < 0.25:
         return
-    var sites = [Vector2(0.17,0.60), Vector2(0.58,0.30)]
-    for site in sites:
-        var p = Vector2(site.x*s.x, site.y*s.y)
-        draw_rect(Rect2(p, Vector2(54,36)), Color("65584a"), true)
-        draw_line(p + Vector2(8,0), p + Vector2(8,-55), Color("d0a04f"), 4.0)
-        draw_line(p + Vector2(8,-52), p + Vector2(58,-52), Color("d0a04f"), 3.0)
-        draw_line(p + Vector2(42,-52), p + Vector2(42,-30), Color("d0a04f"), 2.0)
+    var sites = [Vector2(0.15,0.57), Vector2(0.56,0.35), Vector2(0.69,0.82)]
+    var site_count = clamp(int(1 + growth / 2.0), 1, sites.size())
+    for i in range(site_count):
+        var p = Vector2(sites[i].x*s.x, sites[i].y*s.y)
+        draw_rect(Rect2(p, Vector2(48,31)), Color("65584a"), true)
+        draw_line(p + Vector2(8,0), p + Vector2(8,-48), Color("d0a04f"), 4.0)
+        draw_line(p + Vector2(8,-45), p + Vector2(54,-45), Color("d0a04f"), 3.0)
+        draw_line(p + Vector2(39,-45), p + Vector2(39,-26), Color("d0a04f"), 2.0)
 
 func _draw_water_activity(s: Vector2) -> void:
-    for i in range(4):
-        var p = Vector2(s.x * (0.10 + i * 0.22), s.y * (0.07 + (i%2)*0.035))
+    for i in range(5):
+        var t = fmod(world_time * (0.45 + float(i) * 0.08) + float(i) * 0.21, 1.0)
+        var p = Vector2(s.x * t, s.y * (0.055 + (i%2)*0.045))
         draw_colored_polygon(PackedVector2Array([p+Vector2(-10,0),p+Vector2(10,0),p+Vector2(6,5),p+Vector2(-6,5)]), Color("d7dde0"))
-        draw_line(p + Vector2(-16,7), p + Vector2(-34,7), Color(WATER_LIGHT,0.55), 1.2)
-
-func _draw_waterfront(s: Vector2) -> void:
-    draw_rect(Rect2(0, 0, s.x, s.y * 0.17), WATER, true)
-    for i in range(12):
-        var y = s.y * (0.025 + float(i) * 0.011)
-        draw_line(Vector2(0, y), Vector2(s.x, y + sin(float(i)) * 4.0), Color(WATER_LIGHT, 0.34), 1.5)
-    draw_rect(Rect2(0, s.y * 0.165, s.x, s.y * 0.025), Color("658374"), true)
-    draw_line(Vector2(0, s.y * 0.19), Vector2(s.x, s.y * 0.19), Color("92a697"), 2.0)
-
-    for bx in [0.18, 0.52, 0.84]:
-        var x = s.x * bx
-        draw_rect(Rect2(x - 25, s.y * 0.02, 50, s.y * 0.18), Color("3c464e"), true)
-        draw_rect(Rect2(x - 20, s.y * 0.02, 40, s.y * 0.18), Color("59646e"), true)
-        for j in range(5):
-            draw_line(Vector2(x - 18, s.y * (0.04 + j * 0.03)), Vector2(x + 18, s.y * (0.04 + j * 0.03)), Color("7f8b92"), 1.0)
-
-func _draw_ground(s: Vector2) -> void:
-    draw_rect(Rect2(0, s.y * 0.19, s.x, s.y * 0.81), Color("3f5146"), true)
-    for i in range(22):
-        var x = fmod(float(i * 137), s.x)
-        var y = s.y * 0.21 + fmod(float(i * 89), s.y * 0.73)
-        draw_circle(Vector2(x, y), 28.0 + float(i % 4) * 9.0, Color(Color("49604c"), 0.22))
-
-func _draw_roads(s: Vector2) -> void:
-    for y_norm in [0.465, 0.745]:
-        var y = s.y * y_norm
-        draw_rect(Rect2(0, y - 22, s.x, 44), Color("7d8589"), true)
-        draw_rect(Rect2(0, y - 18, s.x, 36), ROAD, true)
-        draw_line(Vector2(0, y), Vector2(s.x, y), Color(Color("c2b37b"), 0.74), 1.5)
-    for x_norm in [0.24, 0.485, 0.73]:
-        var x = s.x * x_norm
-        draw_rect(Rect2(x - 22, s.y * 0.17, 44, s.y * 0.83), Color("7d8589"), true)
-        draw_rect(Rect2(x - 18, s.y * 0.17, 36, s.y * 0.83), ROAD, true)
-        draw_line(Vector2(x, s.y * 0.17), Vector2(x, s.y), Color(Color("c2b37b"), 0.74), 1.5)
-
-func _draw_roads_foreground(s: Vector2) -> void:
-    for y_norm in [0.465, 0.745]:
-        var y = s.y * y_norm
-        for i in range(20):
-            var x = s.x * (0.02 + float(i) * 0.052)
-            draw_line(Vector2(x, y - 2), Vector2(x + 14, y - 2), Color(Color("c4c9cc"), 0.45), 1.0)
-    for x_norm in [0.24, 0.485, 0.73]:
-        var x = s.x * x_norm
-        for i in range(16):
-            var y = s.y * (0.21 + float(i) * 0.05)
-            draw_line(Vector2(x - 2, y), Vector2(x - 2, y + 11), Color(Color("c4c9cc"), 0.45), 1.0)
-
-    # Crosswalks at the main intersections.
-    for x_norm in [0.24, 0.485, 0.73]:
-        for y_norm in [0.465, 0.745]:
-            var c = Vector2(s.x * x_norm, s.y * y_norm)
-            for stripe in range(-3, 4):
-                draw_rect(Rect2(c.x - 27, c.y + stripe * 5 - 1.5, 13, 2.8), Color(Color("e0e2dd"), 0.75), true)
-                draw_rect(Rect2(c.x + 14, c.y + stripe * 5 - 1.5, 13, 2.8), Color(Color("e0e2dd"), 0.75), true)
-
-func _draw_district(s: Vector2, d: Dictionary) -> void:
-    var r_norm = d.rect
-    var rect = Rect2(Vector2(r_norm.position.x * s.x, r_norm.position.y * s.y), Vector2(r_norm.size.x * s.x, r_norm.size.y * s.y))
-    var base: Color = d.color
-    draw_rect(rect, Color(base, 0.18), true)
-
-    var growth = float(economy.get("gdp_growth", 2.0))
-    var unemployment = float(economy.get("unemployment", 5.0))
-    var health = clamp(0.82 + growth * 0.025 - max(0.0, unemployment - 5.0) * 0.018, 0.48, 1.05)
-    var count = int(d.density)
-    var cols = 5 if rect.size.x > 280 else 4
-    var rows = int(ceil(float(count) / float(cols)))
-    var cell_w = rect.size.x / float(cols)
-    var cell_h = rect.size.y / float(max(1, rows))
-    for i in range(count):
-        var col = i % cols
-        var row = int(i / cols)
-        var hash_a = float((i * 47 + int(d.seed) * 13) % 100) / 100.0
-        var hash_b = float((i * 31 + int(d.seed) * 29) % 100) / 100.0
-        var bw = cell_w * (0.42 + hash_a * 0.26)
-        var bh = cell_h * (0.42 + hash_b * 0.28)
-        var px = rect.position.x + cell_w * float(col) + cell_w * 0.13 + hash_b * cell_w * 0.10
-        var py = rect.position.y + cell_h * float(row) + cell_h * 0.15 + hash_a * cell_h * 0.08
-        var footprint = Rect2(px, py, bw, bh)
-        var tall = 1.0
-        if d.name == "Financial District" or d.name == "Tech Quarter":
-            tall = 1.35
-        if d.name == "Lakeside Suburbs" or d.name == "West Residential":
-            tall = 0.72
-        _draw_building(footprint, base, hash_a, health, tall)
-
-func _draw_building(rect: Rect2, base: Color, hash_value: float, health: float, height_factor: float) -> void:
-    var lift = (5.0 + hash_value * 13.0) * height_factor
-    var shadow = Rect2(rect.position + Vector2(7, 8), rect.size)
-    draw_rect(shadow, Color(Color("0b1116"), 0.34), true)
-
-    var facade = base.lightened(0.04 + hash_value * 0.12)
-    if health < 0.6 and hash_value < 0.35:
-        facade = facade.darkened(0.22)
-    draw_rect(rect, facade, true)
-    draw_rect(rect, base.lightened(0.28), false, 1.0)
-
-    var roof = PackedVector2Array([
-        rect.position,
-        rect.position + Vector2(lift, -lift * 0.46),
-        rect.position + Vector2(rect.size.x + lift, -lift * 0.46),
-        rect.position + Vector2(rect.size.x, 0)
-    ])
-    draw_colored_polygon(roof, base.lightened(0.22))
-    var side = PackedVector2Array([
-        rect.position + Vector2(rect.size.x, 0),
-        rect.position + Vector2(rect.size.x + lift, -lift * 0.46),
-        rect.position + Vector2(rect.size.x + lift, rect.size.y - lift * 0.46),
-        rect.position + Vector2(rect.size.x, rect.size.y)
-    ])
-    draw_colored_polygon(side, base.darkened(0.18))
-
-    if rect.size.x > 28 and rect.size.y > 20:
-        for wx in range(2):
-            for wy in range(2):
-                var wpos = rect.position + Vector2(7 + wx * max(10.0, rect.size.x * 0.42), 7 + wy * max(8.0, rect.size.y * 0.38))
-                draw_rect(Rect2(wpos, Vector2(4.0, 3.0)), Color(Color("d6c98a"), 0.55 if health > 0.65 else 0.22), true)
+        draw_line(p + Vector2(-12,7), p + Vector2(-28,7), Color(WATER_LIGHT,0.55), 1.2)
 
 func _draw_central_park(s: Vector2) -> void:
-    var rect = Rect2(s.x * 0.265, s.y * 0.515, s.x * 0.195, s.y * 0.19)
+    var rect = Rect2(s.x * 0.265, s.y * 0.515, s.x * 0.18, s.y * 0.15)
     draw_rect(rect, PARK, true)
     draw_rect(rect, Color(Color("a6b28d"), 0.8), false, 1.2)
-    draw_line(rect.position + Vector2(0, rect.size.y * 0.5), rect.position + Vector2(rect.size.x, rect.size.y * 0.5), Color(Color("b7aa88"), 0.75), 7.0)
-    draw_line(rect.position + Vector2(rect.size.x * 0.5, 0), rect.position + Vector2(rect.size.x * 0.5, rect.size.y), Color(Color("b7aa88"), 0.75), 7.0)
+    draw_line(rect.position + Vector2(0, rect.size.y * 0.5), rect.position + Vector2(rect.size.x, rect.size.y * 0.5), Color(Color("b7aa88"), 0.75), 6.0)
+    draw_line(rect.position + Vector2(rect.size.x * 0.5, 0), rect.position + Vector2(rect.size.x * 0.5, rect.size.y), Color(Color("b7aa88"), 0.75), 6.0)
     var center = rect.get_center()
-    draw_circle(center, 19, Color("b5c8cc"))
-    draw_circle(center, 12, Color("3f7285"))
-    draw_circle(center, 4, Color("d8e3e4"))
-    for i in range(22):
-        var px = rect.position.x + 15 + fmod(float(i * 43), max(20.0, rect.size.x - 30))
-        var py = rect.position.y + 14 + fmod(float(i * 67), max(20.0, rect.size.y - 28))
-        if abs(px - center.x) < 25 or abs(py - center.y) < 15:
+    draw_circle(center, 17, Color("b5c8cc"))
+    draw_circle(center, 10, Color("3f7285"))
+    for i in range(26):
+        var px = rect.position.x + 12 + fmod(float(i * 43), max(20.0, rect.size.x - 24))
+        var py = rect.position.y + 12 + fmod(float(i * 67), max(20.0, rect.size.y - 24))
+        if abs(px - center.x) < 22 or abs(py - center.y) < 13:
             continue
-        _draw_tree(Vector2(px, py), 6.0 + float(i % 3))
+        _draw_tree(Vector2(px, py), 5.2 + float(i % 3))
 
 func _draw_tree(pos: Vector2, radi