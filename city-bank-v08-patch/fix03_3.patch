0.09))
+    for crown_data in [Vector3(0,3.0,0), Vector3(-0.45,3.45,0.20), Vector3(0.48,3.35,-0.18)]:
+        var crown_mesh = SphereMesh.new()
+        crown_mesh.radius = 0.92 * scale_factor
+        crown_mesh.height = 1.85 * scale_factor
+        var crown = MeshInstance3D.new()
+        crown.mesh = crown_mesh
+        crown.position = crown_data * scale_factor
+        crown.scale = Vector3(1.0,0.82,1.0)
+        crown.material_override = _material(leaf_base.lightened(rng.randf_range(-0.035,0.05)), 0.98)
+        root.add_child(crown)
 
 func _add_university_field() -> void:
     var center = _norm_to_world(Vector2(0.86, 0.39), 0.14)
@@ -412,10 +588,86 @@
         var mast = _box(detail_root, Vector3(0.35,12.0,0.35), base + Vector3(-2.5,6.0,0), Color("d2a53c"), "CraneMast", 0.7, 0.2)
         _box(mast, Vector3(8.0,0.28,0.28), Vector3(3.5,5.5,0), Color("d2a53c"), "CraneBoom", 0.7, 0.2)
 
+func _add_street_trees() -> void:
+    for ix in range(road_xs.size()):
+        if ix % 2 == 0:
+            var x = _world_x(float(road_xs[ix])) + ROAD_WIDTH * 0.72 + SIDEWALK_WIDTH * 0.32
+            for z in range(-54, 57, 12):
+                if abs(float(z) - _world_z(0.105)) > 8.0:
+                    _add_tree(Vector3(x, 0.2, float(z)), 0.62 + rng.randf() * 0.22)
+    for iy in range(road_ys.size()):
+        if iy % 2 == 1:
+            var z = _world_z(float(road_ys[iy])) - ROAD_WIDTH * 0.72 - SIDEWALK_WIDTH * 0.32
+            for x in range(-78, 79, 14):
+              