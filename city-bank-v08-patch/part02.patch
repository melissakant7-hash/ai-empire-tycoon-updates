r bd = max(3.5, cell_v * WORLD_DEPTH * (0.50 + h2 * 0.12))
+            var u = rect.position.x + float(col) * cell_u + cell_u * (0.14 + h2 * 0.04)
+            var v = rect.position.y + float(row) * cell_v + cell_v * (0.14 + h1 * 0.04)
+            var bw = max(3.2, cell_u * WORLD_WIDTH * (0.56 + h1 * 0.13))
+            var bd = max(3.2, cell_v * WORLD_DEPTH * (0.54 + h2 * 0.13))
             var kind = _building_kind_for_district(String(d.name), i)
             var height_factor = _building_height_for_district(String(d.name), h2)
             var height = _height_for_kind(kind) * height_factor
-            var pos = Vector3(_world_x(u + cell_u * 0.34), height * 0.5 + 0.22, _world_z(v + cell_v * 0.34))
+            var world_x = _world_x(u + cell_u * 0.38)
+            var world_z = _world_z(v + cell_v * 0.38)
             var base_color = _building_color(kind, district_index, h1)
+            _box(building_root, Vector3(bw + 0.9, 0.16, bd + 0.9), Vector3(world_x, 0.18, world_z), Color("6e706d").lerp(Color(d.color), 0.10), "BuildingLot", 0.96)
+            var pos = Vector3(world_x, height * 0.5 + 0.26, world_z)
             var body = _box(building_root, Vector3(bw, height, bd), pos, base_color, "%s_%03d" % [String(d.name).replace(" ", "_"), i], 0.76, 0.04 if kind == "office" else 0.0)
-            var roof_h = 0.45 if height < 11.0 else 0.8
-            _box(body, Vector3(bw * 0.42, roof_h, bd * 0.45), Vector3(0, height * 0.5 + roof_h * 0.5, 0), base_color.lightened(0.08), "Roof")
-            if kind == "office" or kind == "retail" or kind == "mixed":
-                _add_building_window_band(body, bw, height, bd, h1)
-            if kind == "industrial":
-                _add_industrial_detail(body, bw, height, bd, i)
+            body.material_override = _facade_material(kind, i % 3)
+            _decorate_building(body, kind, bw, height, bd, i, district_index)
             buildings.append({"node":body, "district":district_index, "kind":kind, "occupied":0.78 + h2 * 0.22, "base_height":height})
 
     _add_bank_headquarters()
+    _add_landmarks()
+
+func _decorate_building(body: MeshInstance3D, kind: String, bw: float, height: float, bd: float, index: int, district_index: int) -> void:
+    var roof_color = _building_color(kind, district_index, 0.55).lightened(0.09)
+    if kind == "residential" and height < 9.5:
+        var left_roof = _box(body, Vector3(bw * 0.58, 0.42, bd * 1.02), Vector3(-bw * 0.24, height * 0.5 + 0.38, 0), Color("51463f"), "PitchedRoofL", 0.94)
+        left_roof.rotation_degrees.z = -18.0
+        var right_roof = _box(body, Vector3(bw * 0.58, 0.42, bd * 1.02), Vector3(bw * 0.24, height * 0.5 + 0.38, 0), Color("51463f"), "PitchedRoofR", 0.94)
+        right_roof.rotation_degrees.z = 18.0
+        _add_balconies(body, bw, height, bd, index)
+    else:
+        var roof_h = 0.42 if height < 11.0 else 0.75
+        _box(body, Vector3(bw * 0.46, roof_h, bd * 0.44), Vector3(0, height * 0.5 + roof_h * 0.5, 0), roof_color, "RoofPlant", 0.82, 0.08)
+    if kind == "office":
+        _add_rooftop_office_detail(body, bw, height, bd, index)
+    elif kind == "retail":
+        _add_retail_front(body, bw, height, bd, index)
+    elif kind == "industrial":
+        _add_industrial_detail(body, bw, height, bd, index)
+    elif kind == "civic" or kind == "university":
+        _add_public_facade_detail(body, bw, height, bd, kind)
+    elif kind == "mixed":
+        _add_retail_front(body, bw, height, bd, index)
+
+func _add_balconies(body: MeshInstance3D, bw: float, height: float, bd: float, index: int) -> void:
+    var count = min(3, max(1, int(height / 3.0) - 1))
+    for floor in range(count):
+        var y = -height * 0.27 + float(floor) * 2.2
+        var side = -1.0 if (floor + index) % 2 == 0 else 1.0
+        var balcony = _box(body, Vector3(bw * 0.34, 0.12, 0.72), Vector3(side * bw * 0.22, y, -bd * 0.54), Color("888b88"), "Balcony", 0.9, 0.05)
+        _box(balcony, Vector3(bw * 0.34, 0.42, 0.06), Vector3(0, 0.24, -0.34), Color("444c50"), "BalconyRail", 0.65, 0.18)
+
+func _add_rooftop_office_detail(body: MeshInstance3D, bw: float, height: float, bd: float, index: int) -> void:
+    _box(body, Vector3(bw * 0.28, 0.65, bd * 0.24), Vector3(bw * 0.12, height * 0.5 + 0.75, 0), Color("58636a"), "HVAC", 0.75, 0.22)
+    if index % 3 == 0:
+        var mast = _cylinder(body, 0.08, 3.2, Vector3(-bw * 0.22, height * 1.6, 0), Color("8c969b"), "Antenna", 0.5, 0.65)
+        mast.rotation_degrees = Vector3.ZERO
+
+func _add_retail_front(body: MeshInstance3D, bw: float, height: float, bd: float, index: int) -> void:
+    var accent_colors = [Color("b6604e"), Color("4e809b"), Color("6b965f"), Color("c28a45")]
+    var accent: Color = accent_colors[index % accent_colors.size()]
+    var awning = _box(body, Vector3(bw * 0.72, 0.22, 0.75), Vector3(0, -height * 0.5 + 2.0, -bd * 0.55), accent, "ShopAwning", 0.74)
+    awning.rotation_degrees.x = -8.0
+    var sign = _box(body, Vector3(bw * 0.48, 0.7, 0.10), Vector3(0, -height * 0.5 + 3.05, -bd * 0.515), accent.lightened(0.14), "ShopSign", 0.52, 0.06)
+    sign.material_override = _emissive_material(accent.lightened(0.16), 0.38)
+
+func _add_public_facade_detail(body: MeshInstance3D, bw: float, height: float, bd: float, kind: String) -> void:
+    var stone = Color("c3b9a3") if kind == "civic" else Color("bba596")
+    for i in range(4):
+        var x = -bw * 0.35 + float(i) * bw * 0.205
+        _cylinder(body, 0.18, min(4.4, height * 0.45), Vector3(x, -height * 0.5 + 2.25, -bd * 0.53), stone, "FacadeColumn", 0.92)
+    _box(body, Vector3(bw * 0.88, 0.34, 0.50), Vector3(0, -height * 0.5 + 4.55, -bd * 0.52), stone, "FacadeLintel", 0.92)
+
+func _add_landmarks() -> void:
+    var civic = _box(building_root, Vector3(12.0, 14.0, 9.0), Vector3(_world_x(0.355), 7.2, _world_z(0.585)), Color("6f777c"), "CityHall", 0.82, 0.03)
+    civic.material_override = _facade_material("civic", 1)
+    _add_public_facade_detail(civic, 12.0, 