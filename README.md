# AI Training Licensing & Release Endpoint

Current Full Game version: **v10.7.8**.

## v10.7.8

- Adds a Sandbox **Unlimited Datacenter Serving Capacity** rule.
- Sandbox training compute can remain infinite while serving capacity is controlled independently.
- With the rule ON, serving capacity is unlimited and the capacity panel shows clean infinity values instead of artificial trillion-PF/token numbers.
- With the rule OFF, serving capacity comes only from the physical datacenters, accelerator generation, custom AI silicon and leased compute the player actually has.
- Manual Sandbox serving uses the same real capacity constraints as Career: subscription/configuration increases can be blocked when the fleet is full and overload limits commercial traffic.
- The Datacenters / Compute Operations display now separates infinite Sandbox training compute from the real physical serving fleet, so capacity testing is readable.
- Existing Sandbox saves default to unlimited serving to preserve their previous behavior; the rule can be switched off at any time.
- Adds German localization for the new Sandbox serving controls.
- Keeps all v10.7.7 systems: input/output token serving economics, live capacity graph, meaningful model optimization generations, visible persistent rival cyber damage, stronger Custom AI Silicon and rebalanced difficulties.
- Keeps the independent Graphics research tree, Unlock All Research Sandbox cheat, replacement-license fix, hardware-bound AITL2 licensing, live validation, human-voice tutorial narration, shared browser profile and no-blur tutorial.

This public repository provides AI Training release metadata and live license revocation data. The paid Full Game package is **not distributed from this public repository**.

Public files contain no private signing seed, no content master key, and no playable Full Game package.
