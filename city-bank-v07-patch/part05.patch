r c.y in [1, 2, 4, 5]:
+            if c.x in [1,3,5,6] or c.y in [1,2,4,5]:
                 main_candidates.append(c)
         if not main_candidates.is_empty():
             candidates = main_candidates
-    if candidates.is_empty():
-        return Vector2i(ix, iy)
-    return candidates[rng.randi_range(0, candidates.size() - 1)]
-
-func _seed_pedestrians() -> void:
-    pedestrians.clear()
-    for i in range(420):
-        var ix = rng.randi_range(0, road_xs.size() - 1)
-        var iy = rng.randi_range(0, road_ys.size() - 1)
-        var target = Vector2i(rng.randi_range(0, road_xs.size() - 1), rng.randi_range(0, road_ys.size() - 1))
-        var next = _pedestrian_next(ix, iy, target.x, target.y)
-        pedestrians.append({
-            "ix": ix, "iy": iy, "nx": next.x, "ny": next.y,
-            "tx": target.x, "ty": target.y,
-            "progress": rng.randf(),
-            "speed": 0.08 + rng.randf_range(0.0, 0.08),
-            "phase": rng.randf(),
-            "side": -1.0 if i % 2 == 0 else 1.0,
-            "purpose": ["work", "shopping", "home", "leisure"][i % 4]
-        })
+    if candidates.is_empty(): return Vector2i(ix, iy)
+    return candidates[rng.randi_range(0, candidates.size()-1)]
 
 func _pedestrian_next(ix: int, iy: int, tx: int, ty: int) -> Vector2i:
-    if ix == tx and iy == ty:
-        return Vector2i(ix, iy)
+    if ix == tx and iy == ty: return Vector2i(ix, iy)
     var choices: Array[Vector2i] = []
-    if ix < tx: choices.append(Vector2i(ix + 1, iy))
-    elif ix > tx: choices.append(Vector2i(ix - 1, iy))
-    if iy < ty: choices.append(Vector2i(ix, iy + 1))
-    elif iy > ty: choices.append(Vector2i(ix, iy - 1))
-    if choices.is_empty():
-        return Vector2i(ix, iy)
-    return choices[rng.randi_range(0, choices.size() - 1)]
+    if ix < tx: choices.append(Vector2i(ix+1,iy))
+    elif ix > tx: choices.append(Vector2i(ix-1,iy))
+    if iy < ty: choices.append(Vector2i(ix,iy+1))
+    elif iy > ty: choices.append(Vector2i(ix,iy-1))
+    if choices.is_empty(): return Vector2i(ix,iy)
+    return choices[rng.randi_range(0, choices.size()-1)]
 
 func _process(delta: float) -> void:
-    world_time = fmod(world_time + delta / 165.0, 1.0)
+    world_time = fmod(world_time + delta / 185.0, 1.0)
+    _update_day_night()
+    _move_vehicles(delta)
+    _move_citizens(delta)
+
+func _move_vehicles(delta: float) -> void:
     for i in range(cars.size()):
         var car = cars[i]
         var progress = float(car.progress) + delta * float(car.speed)
@@ -197,7 +521,17 @@
             car.nx = next.x
             car.ny = next.y
         car.progress = progress
+        var a = _road_point(int(car.ix), int(car.iy))
+        var b = _road_point(int(car.nx), int(car.ny))
+        var dir = (b - a).normalized()
+        var lane_offset = Vector3(-dir.z, 0, dir.x) * (0.82 if (i%2)==0 else -0.82)
+        var node: Node3D = car.node
+        node.position = a.lerp(b, progress) + lane_offset + Vector3(0,0.13,0)
+        if dir.length() > 0.1:
+            node.rotation.y = atan2(dir.x, dir.z)
         cars[i] = car
+
+func _move_citizens(delta: float) -> void:
     for i in range(pedestrians.size()):
         var person = pedestrians[i]
         var progress = float(person.progress) + delta * float(person.speed)
@@ -206,481 +540,195 @@
             person.ix = int(person.nx)
             person.iy = int(person.ny)
             if int(person.ix) == int(person.tx) and int(person.iy) == int(person.ty):
-                person.tx = rng.randi_range(0, road_xs.size() - 1)
-                person.ty = rng.randi_range(0, road_ys.size() - 1)
-                person.purpose = ["work", "shopping", "home", "leisure"][rng.randi_range(0, 3)]
+                person.tx = rng.randi_range(0, road_xs.size()-1)
+                person.ty = rng.randi_range(0, road_ys.size()-1)
+                person.purpose = ["work","shopping","home","leisure"][rng.randi_range(0,3)]
             var next = _pedestrian_next(int(person.ix), int(person.iy), int(person.tx), int(person.ty))
             person.nx = next.x
             person.ny = next.y
         person.progress = progress
+        var a = _road_point(int(person.ix), int(person.iy))
+        var b = _road_point(int(person.nx), int(person.ny))
+        var dir = (b-a).normalized()
+        var sidewalk_offset = Vector3(-dir.z,0,dir.x) * (ROAD_WIDTH*0.64 + SIDEWALK_WIDTH*0.35) * float(person.side)
+        var node: Node3D = person.node
+        node.position = a.lerp(b, progress) + sidewalk_offset + Vector3(0,0.15,0)
+        if dir.length() > 0.1:
+            node.rotation.y = atan2(dir.x, dir.z)
         pedestrians[i] = person
-    queue_redraw()
+
+func _update_day_night() -> void:
+    if not is_instance_valid(sun): return
+    var daylight = clamp(sin(world_time * TAU - 0.55) * 0.58 + 0.58, 0.08, 1.0)
+    sun.light_energy = 0.18 + daylight * 1.35
+    sun.rotation_degrees = Vector3(-18.0 - daylight * 48.0, -35.0 + world_time * 35.0, 0.0)
+    if