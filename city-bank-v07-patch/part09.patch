ct2(Vector2(body.get_center().x - 3, body.end.y - 9), Vector2(6, 9)), Color("4e4038"), true)
-
-func _draw_retail(rect: Rect2, base: Color, seed_value: float, occupancy: float) -> void:
-    draw_rect(rect, base.lightened(0.12), true)
-    draw_rect(Rect2(rect.position + Vector2(0, rect.size.y * 0.60), Vector2(rect.size.x, rect.size.y * 0.40)), Color("354852"), true)
-    var awning = Color("c76a55") if seed_value < 0.5 else Color("d0a14d")
-    draw_rect(Rect2(rect.position + Vector2(0, rect.size.y * 0.52), Vector2(rect.size.x, 4)), awning, true)
-    if occupancy < 0.55:
-        draw_line(rect.position + Vector2(3, rect.size.y * 0.68), rect.end - Vector2(3, 3), Color("b34f4f"), 2.0)
-        draw_line(Vector2(rect.end.x - 3, rect.position.y + rect.size.y * 0.68), Vector2(rect.position.x + 3, rect.end.y - 3), Color("b34f4f"), 2.0)
-
-func _draw_warehouse(rect: Rect2, base: Color, seed_value: float, occupancy: float) -> void:
-    var body = Rect2(rect.position + Vector2(0, rect.size.y * 0.18), Vector2(rect.size.x, rect.size.y * 0.82))
-    draw_rect(body, base.lightened(0.08), true)
-    var roof = PackedVector2Array([
-        Vector2(rect.position.x, body.position.y),
-        Vector2(rect.position.x + rect.size.x * 0.25, rect.position.y),
-        Vector2(rect.position.x + rect.size.x * 0.50, body.position.y),
-        Vector2(rect.position.x + rect.size.x * 0.75, rect.position.y),
-        Vector2(rect.end.x, body.position.y)
-    ])
-    draw_polyline(roof, Color("a5a59a"), 2.0)
-    draw_rect(Rect2(Vector2(body.position.x + 5, body.end.y - 10), Vector2(max(10.0, body.size.x * 0.30), 10)), Color("343c3f"), true)
-    if occupancy < 0.50:
-        draw_rect(body, Color(0.12,0.10,0.08,0.22), true)
-
-func _draw_city_details(s: Vector2) -> void:
-    _draw_street_trees(s)
-    _draw_transit_stops(s)
-    _draw_parking_and_plazas(s)
-    _draw_industry_details(s)
-    _draw_university_details(s)
-    _draw_construction(s)
-    _draw_water_activity(s)
-    _draw_traffic_signals(s)
-
-func _draw_waterfront(s: Vector2) -> void:
-    draw_rect(Rect2(0, 0, s.x, s.y * 0.18), WATER, true)
-    for i in range(14):
-        var y = s.y * (0.02 + float(i) * 0.010)
-        draw_line(Vector2(0, y), Vector2(s.x, y + sin(float(i) + world_time * TAU) * 4.0), Color(WATER_LIGHT, 0.28), 1.4)
-    draw_rect(Rect2(0, s.y * 0.165, s.x, s.y * 0.035), Color("66877a"), true)
-    for ix in [1, 3, 5]:
-        var x = s.x * float(road_xs[ix])
-        draw_rect(Rect2(x - 20, s.y * 0.025, 40, s.y * 0.19), Color("3b444c"), true)
-        draw_rect(Rect2(x - 16, s.y * 0.025, 32, s.y * 0.19), Color("58636d"), true)
-        for j in range(6):
-            draw_line(Vector2(x - 14, s.y * (0.04 + j * 0.027)), Vector2(x + 14, s.y * (0.04 + j * 0.027)), Color("89959c"), 1.0)
-
-func _draw_ground(s: Vector2) -> void:
-    draw_rect(Rect2(0, s.y * 0.19, s.x, s.y * 0.81), GRASS, true)
-    for i in range(30):
-        var x = fmod(float(i * 137), s.x)
-        var y = s.y * 0.21 + fmod(float(i * 89), s.y * 0.73)
-        draw_circle(Vector2(x, y), 22.0 + float(i % 5) * 8.0, Color(Color("526c55"), 0.17))
-
-func _draw_roads(s: Vector2) -> void:
-    for yi in range(road_ys.size()):
-        var y = s.y * float(road_ys[yi])
-        var main = yi in [2, 4]
-        var road_w = 38.0 if main else 24.0
-        draw_rect(Rect2(0, y - road_w * 0.5 - 5, s.x, road_w + 10), SIDEWALK, true)
-        draw_rect(Rect2(0, y - road_w * 0.5, s.x, road_w), ROAD, true)
-        draw_line(Vector2(0, y), Vector2(s.x, y), Color(LANE, 0.70 if main else 0.38), 1.3)
-    for xi in range(road_xs.size()):
-        var x = s.x * float(road_xs[xi])
-        var main = xi in [1, 3, 5]
-        var road_w = 38.0 if main else 24.0
-        draw_rect(Rect2(x - road_w * 0.5 - 5, s.y * 0.19, road_w + 10, s.y * 0.81), SIDEWALK, true)
-        draw_rect(Rect2(x - road_w * 0.5, s.y * 0.19, road_w, s.y * 0.81), ROAD, true)
-        draw_line(Vector2(x, s.y * 0.19), Vector2(x, s.y), Color(LANE, 0.70 if main else 0.38), 1.3)
 
-func _draw_roads_foreground(s: Vector2) -> void:
-    for ix in range(road_xs.size()):
-        for iy in range(road_ys.size()):
-            var c = _road_point(ix, iy, s)
-            var main_intersection = ix in [1,3,5] or iy in [2,4]
-            if not main_intersection:
-                continue
-            for stripe in range(-2, 3):
-                draw_rect(Rect2(c.x - 20, c.y + stripe * 4 - 1, 9, 2.2), Color(Color("e0e2dd"), 0.70), true)
-                draw_rect(Rect2(c.x + 11, c.y + stripe * 4 - 1, 9, 2.2), Color(Color("e0e2dd"), 0.70), true)
+func _road_point(ix: int, iy: int) -> Vector3:
+    return Vector3(_world_x(float(road_xs[ix])), 0.33, _world_z(float(road_ys[iy])))
 
-func _draw_street_trees(s: Vector2) -> void:
-    for ix in range(road_xs.size()):
-        if ix % 2 == 0:
-            continue
-        var x = s.x * float(road_xs[ix])
-        for j in range(10):
-            var y = s.y * (0.23 + float(j) * 0.068)
-           