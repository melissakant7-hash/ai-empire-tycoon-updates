35.0 - migration_rate * 30.0 + rng.randf_range(-0.08, 0.08), 0.5, 18.0)
     city.foreclosures = max(0.0, city.foreclosures * 0.82 + _count_mortgage_delinquencies() * rng.randf_range(2.0, 8.0))
 
-    city.happiness = clamp(city.happiness + city.gdp_growth * 0.06 - max(0.0, city.unemployment - 5.0) * 0.12 - fee_pressure * 0.16 - max(0.0, policy.income_tax - 22.0) * 0.06 - max(0.0, city.inflation - 4.0) * 0.10, 5.0, 95.0)
-    city.poverty = clamp(city.poverty + max(0.0, city.unemployment - 6.0) * 0.05 + max(0.0, city.inflation - 4.0) * 0.025 - max(0.0, city.gdp_growth) * 0.025, 2.0, 45.0)
-    city.inequality = clamp(city.inequality + max(0.0, 15.0 - policy.income_tax) * 0.018 - max(0.0, policy.income_tax - 25.0) * 0.012, 18.0, 75.0)
-    city.crime = clamp(city.crime + (city.poverty - 10.0) * 0.015 - city.happiness * 0.002, 5.0, 90.0)
-
-    var taxable_income = city.gdp * 0.43
-    var tax_revenue = taxable_income * policy.income_tax / 100.0 / 12.0 + city.gdp * 0.11 * policy.corporate_tax / 100.0 / 12.0 + city.gdp * 0.46 * policy.sales_tax / 100.0 / 12.0
-    tax_revenue += city.gdp * 0.05 * policy.capital_gains_tax / 100.0 / 12.0 + city.gdp * 0.02 * policy.luxury_tax / 100.0 / 12.0
-    var city_spend = city.population * 960.0 / 12.0
-    city.city_budget += tax_revenue - city_spend
+    var health_target = clamp(48.0 + float(government.healthcare_budget) * 0.22 + float(government.healthcare_subsidy) * 0.07 + float(government.sanitation_budget) * 0.06 - city.poverty * 0.35, 15.0, 98.0)
+    var education_target = clamp(43.0 + float(government.education_budget) * 0.30 + float(government.social_services_budget) * 0.05 - city.poverty * 0.18, 15.0, 98.0)
+    var fire_target = clamp(48.0 + float(government.fire_budget) * 0.30 + float(government.roads_budget) * 0.10, 15.0, 99.0)
+    var cleanliness_target = clamp(42.0 + float(government.sanitation_budget) * 0.28 + float(government.parks_budget) * 0.12, 15.0, 99.0)
+    var traffic_target = clamp(43.0 + max(0.0, city.