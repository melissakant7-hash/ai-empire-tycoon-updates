delinquency, _money(_monthly_tax_revenue_estimate() * district_factor), city.health, 100.0 - city.crime,
             _money(city.housing_price * (0.86 + district_factor)), _money(city.avg_rent * (0.90 + district_factor)), city.gdp_growth, 100.0 - district_unemployment, bank.reputation
         ]
     if is_instance_valid(bottom_alerts_label):
@@ -501,11 +601,11 @@ GDP %.1f%%   •   Jobs %.1f%%   •   Trust %.0f%%" % [
 • %d unread mail messages
 • %d loan applications require review" % [_unread_notification_count(), _mail_unread_count(), applications.size()]
     if is_instance_valid(bottom_economy_label):
-        bottom_economy_label.text = "GDP Growth  %.1f%%     Inflation  %.1f%%     Unemployment  %.1f%%     Confidence  %.0f" % [city.gdp_growth, city.inflation, city.unemployment, city.happiness + 32.0]
+        bottom_economy_label.text = "GDP %.1f%%   Inflation %.1f%%   Unemployment %.1f%%   Health %.0f   Crime %.0f   Traffic %.0f" % [city.gdp_growth, city.inflation, city.unemployment, city.health, city.crime, city.traffic]
     if is_instance_valid(bottom_city_label):
-        bottom_city_label.text = "Population  %s
-Median Income  %s
-Businesses  %s" % [_fmt_int(city.population), _money(city.median_salary), _fmt_int(city.businesses)]
+        bottom_city_label.text = "Population %s
+Treasury %s   Debt %s
+Services %s/mo" % [_fmt_int(city.population), _money(city.city_budget), _money(city.city_debt), _money(_monthly_city_service_spending_estimate())]
 
 func _count_stressed_businesses() -> int:
     var count = 0
@@ -652,11 +752,12 @@ func _show_view(name: String) -> void:
         "Customers": view = _overlay_panel(_view_customers())
         "Businesses": view = _overlay_panel(_view_businesses())
         "Bank Policies": view = _overlay_panel(_view_bank_policies())
-        "Taxes": view = _overlay_panel(_view_taxes())
+        "City Hall": view = _overlay_panel(_view_city_hall())
+        "Taxes", "Taxes & Budget": view = _overlay_panel(_view_taxes())
+        "City Services": view = _overlay_panel(_view_city_services())
         "City Economy": view = _overlay_panel(_view_city())
         "Housing": view = _overlay_panel(_view_housing())
         "City Map": view = _view_city_map_overlay()
-        "Competitors": view = _overlay_panel(_view_competitors())
         "Reports": view = _overlay_panel(_view_reports())
         "News": view = _overlay_panel(_view_news())
         "Settings": view = _overlay_panel(_view_settings())
@@ -775,200 +876,189 @@ func _dashboard_news() -> Control:
     return p
 
 func _view_mail() -> Control:
-    var outer = VBoxContainer.new()
-    outer.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-    outer.size_flags_vertical = Control.SIZE_EXPAND_FILL
-    outer.add_theme_constant_override("separation", 10)
-
-    var header = HBoxContainer.new()
-    header.add_theme_constant_override("separation", 10)
-    outer.add_child(header)
-    var title = Label.new()
-    title.text = "Mail"
-    title.add_theme_font_size_override("font_size", 28)
-    title.add_theme_color_override("font_color", C_TEXT)
-    header.add_child(title)
+    var shell = PanelContainer.new()
+    shell.size_flags_horizontal = Control.SIZE_EXPAND_FILL
+    shell.size_flags_vertical = Control.SIZE_EXPAND_FILL
+    shell.add_theme_stylebox_override("panel", _panel_style(Color("f6f8fc"), 10, Color("d8e0ea")))
+    var root = VBoxContainer.new()
+    root.add_theme_constant_override("separation", 0)
+    shell.add_child(root)
+
+    var top = PanelContainer.new()
+    top.custom_minimum_size.y = 58
+    top.add_theme_stylebox_override("panel", _panel_style(Color("ffffff"), 0, Color("e1e6ec")))
+    root.add_child(top)
+    var th = HBoxContainer.new()
+    th.add_theme_constant_override("separation", 12)
+    top.add_child(th)
+    var brand = Label.new()
+    brand.text = "  ✉  CityMail"
+    brand.custom_minimum_size.x = 190
+    brand.add_theme_font_size_override("font_size", 19)
+    brand.add_theme_color_override("font_color", Color("3c4043"))
+    brand.vertical_alignment = VERTICAL_ALIGNMENT_CENTER
+    th.add_child(brand)
+    var search = LineEdit.new()
+    search.placeholder_text = "Search mail"
+    search.text = mail_search_query
+    search.size_flags_horizontal = Control.SIZE_EXPAND_FILL
+    search.custom_minimum_size.y = 42
+    search.add_theme_color_override("font_color", Color("202124"))
+    search.add_theme_color_override("font_placeholder_color", Color("6f7780"))
+    search.add_theme_stylebox_override("normal", _panel_style(Color("eaf1fb"), 18, Color.TRANSPARENT))
+    search.text_submitted.connect(func(value): mail_search_query = value.strip_edges(); selected_mail_id = -1; _show_view("Mail"))
+    th.add_child(search)
     var unread = Label.new()
     unread.text = "%d unread" % _mail_unread_count()
-    unread.add_theme_color_override("font_color", C_BLUE)
+    unread.custom_minimum_size.x = 90
+    unread.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
     unread.vertical_alignment = VERTICAL_ALIGNMENT_CENTER
-    header.add_child(unread)
-    var header_space = Control.new()
-    header_space.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-    header.add_child(header_space)
-    var search_hint = LineEdit.new()
-    search_hint.placeholder_text = "Search mail"
-    search_hint.custom_minimum_size.x = 210
-    header.add_child(search_hint)
-
-    var row = HBoxContainer.new()
-    row.size_flags_vertical = Control.SIZE_EXPAND_FILL
-    row.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-    row.add_theme_constant_override("separation", 8)
-    row.clip_contents = true
-    outer.add_child(row)
-
-    var folders = PanelContainer.new()
-    folders.custom_minimum_size.x = 138
-    folders.add_theme_stylebox_override("panel", _panel_style(Color(0.025,0.045,0.065,0.96), 8, Color("243441")))
-    row.add_child(folders)
-    var fv = VBoxContainer.new()
-    fv.add_theme_constant_override("separation", 4)
-    folders.add_child(fv)
-    var compose = _button("+ Compose", 130)
-    compose.tooltip_text = "Outgoing replies are created from an application message."
-    fv.add_child(compose)
-    for folder_name in ["Inbox", "Loan Applications", "Starred", "Sent", "Archive"]:
+    unread.add_theme_color_override("font_color", Color("5f6368"))
+    th.add_child(unread)
+
+    var body_row = HBoxContainer.new()
+    body_row.size_flags_vertical = Control.SIZE_EXPAND_FILL
+    body_row.add_theme_constant_override("separation", 0)
+    root.add_child(body_row)
+
+    var nav = PanelContainer.new()
+    nav.custom_minimum_size.x = 190
+    nav.add_theme_stylebox_override("panel", _panel_style(Color("f6f8fc"), 0, Color.TRANSPARENT))
+    body_row.add_child(nav)
+    var nv = VBoxContainer.new()
+    nv.add_theme_constant_override("separation", 4)
+    nav.add_child(nv)
+    var compose = Button.new()
+    compose.text = "✎  Compose"
+    compose.custom_minimum_size = Vector2(155, 48)
+    compose.add_theme_font_size_override("font_size", 13)
+    compose.add_theme_color_override("font_color", Color("1f1f1f"))
+    compose.add_theme_stylebox_override("normal", _panel_style(Color("c2e7ff"), 16, Color.TRANSPARENT))
+    compose.add_theme_stylebox_override("hover", _panel_style(Color("afdafa"), 16, Color.TRANSPARENT))
+    nv.add_child(compose)
+    for folder_name in ["Inbox", "Starred", "Loan Applications", "Sent", "Archive"]:
         var b = Button.new()
-        var count_text = ""
-        if folder_name == "Inbox":
-            count_text = "  %d" % _mail_unread_count()
-        b.text = "%s%s" % [folder_name, count_text]
+        var icon = {"Inbox":"▣", "Starred":"☆", "Loan Applications":"◆", "Sent":"➤", "Archive":"▤"}.get(folder_name, "•")
+        var extra = "  %d" % _mail_unread_count() if folder_name == "Inbox" else ""
+        b.text = "%s   %s%s" % [icon, folder_name, extra]
         b.alignment = HORIZONTAL_ALIGNMENT_LEFT
-        b.custom_minimum_size = Vector2(135, 38)
-        b.add_theme_color_override("font_color", C_TEXT)
-        b.add_theme_stylebox_override("normal", _panel_style(Color.TRANSPARENT, 6, Color.TRANSPARENT))
-        b.add_theme_stylebox_override("hover", _panel_style(Color(0.08,0.14,0.20,0.75), 6, Color("28465b")))
-        if selected_mail_folder == folder_name:
-            b.add_theme_stylebox_override("normal", _panel_style(Color(0.08,0.18,0.28,0.88), 6, C_BLUE))
+        b.custom_minimum_size = Vector2(180, 36)
+        b.add_theme_color_override("font_color", Color("202124"))
+        var base = Color("d3e3fd") if selected_mail_folder == folder_name else Color.TRANSPARENT
+        b.add_theme_stylebox_override("normal", _panel_style(base, 0, Color.TRANSPARENT))
+        b.add_theme_stylebox_override("hover", _panel_style(Color("e8eaed"), 0, Color.TRANSPARENT))
         var f = folder_name
         b.pressed.connect(func(): selected_mail_folder = f; selected_mail_id = -1; _show_view("Mail"))
-        fv.add_child(b)
-
-    var list_panel = PanelContainer.new()
-    list_panel.custom_minimum_size.x = 278
-    list_panel.add_theme_stylebox_override("panel", _panel_style(Color(0.025,0.045,0.065,0.96), 8, Color("243441")))
-    row.add_child(list_panel)
-    var list_scroll = ScrollContainer.new()
-    list_scroll.size_flags_vertical = Control.SIZE_EXPAND_FILL
-    list_scroll.horizontal_scroll_mode = ScrollContainer.SCROLL_MODE_DISABLED
-    list_panel.add_child(list_scroll)
-    var list_v = VBoxContainer.new()
-    list_v.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-    list_v.add_theme_constant_override("separation", 3)
-    list_scroll.add_child(list_v)
-
-    var visible_ids = _mail_ids_for_folder(selected_mail_folder)
-    if visible_ids.is_empty():
-        var empty = Label.new()
-        empty.text = "No messages in this folder."
-        empty.add_theme_color_override("font_color", C_MUTED)
-        list_v.add_child(empty)
+        nv.add_child(b)
+
+    var main = PanelContainer.new()
+    main.size_flags_horizontal = Control.SIZE_EXPAND_FILL
+    main.size_flags_vertical = Control.SIZE_EXPAND_FILL
+    main.add_theme_stylebox_override("panel", _panel_style(Color("ffffff"), 10, Color("e1e6ec")))
+    body_row.add_child(main)
+
+    if selected_mail_id >= 0 and _mail_index_by_id(selected_mail_id) >= 0:
+        main.add_child(_gmail_message_reader(selected_mail_id))
     else:
-        if selected_mail_id == -1:
-            selected_mail_id = int(visible_ids[0])
-        for mid in visible_ids:
-            var mi = _mail_index_by_id(int(mid))
-            if mi < 0:
-                continue
-            var m = mail_messages[mi]
-            var mb = Button.new()
-            mb.custom_minimum_size = Vector2(256, 78)
-            mb.alignment = HORIZONTAL_ALIGNMENT_LEFT
-            var dot = "● " if bool(m.get("unread", false)) else "  "
-            mb.text = "%s%s\n%s\n%s" % [dot, String(m.get("sender_name","Unknown")), String(m.get("subject","")), String(m.get("date_label",""))]
-            mb.add_theme_font_size_override("font_size", 11)
-            mb.add_theme_color_override("font_color", C_TEXT)
-            var normal_col = Color(0.06,0.12,0.17,0.94) if int(m.id) == selected_mail_id else Color(0.035,0.065,0.09,0.60)
-            mb.add_theme_stylebox_override("normal", _panel_style(normal_col, 5, C_BLUE if int(m.id) == selected_mail_id else Color.TRANSPARENT))
-            mb.add_theme_stylebox_override("hover", _panel_style(Color(0.08,0.16,0.22,0.95), 5, C_BLUE))
-            var message_id = int(m.id)
-            mb.pressed.connect(func(): _open_mail_message(message_id))
-            list_v.add_child(mb)
-
-    var reader = PanelContainer.new()
-    reader.size_flags_horizontal = Control.SIZE_EXPAND_FILL
-    reader.add_theme_stylebox_override("panel", _panel_style(Color(0.025,0.045,0.065,0.96), 8, Color("243441")))
-    row.add_child(reader)
-    var rv = VBoxContainer.new()
-    rv.add_theme_constant_override("separation", 10)
-    reader.add_ch