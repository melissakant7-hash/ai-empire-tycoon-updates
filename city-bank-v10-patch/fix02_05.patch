ext = "▣  %s\n     %s" % [String(doc.get("name","Attachment")), String(doc.get("status","Received"))]; ab.add_theme_color_override("font_color", Color("202124")); ab.add_theme_stylebox_override("normal", _panel_style(Color("ffffff"), 8, Color("cfd8e3"))); ab.add_theme_stylebox_override("hover", _panel_style(Color("f1f7ff"), 8, Color("7aa9e8"))); var aid = int(app.id); var didx = doc_index; ab.pressed.connect(func(): _open_attachment(aid, didx)); grid.add_child(ab)
+    var actions = HBoxContainer.new(); actions.add_theme_constant_override("separation", 8); body.add_child(actions)
     if not app.is_empty() and _active_application_index_by_id(int(app.id)) >= 0:
-        var reply = _button("Reply", 95)
-        reply.pressed.connect(func(): _send_application_reply(int(app.id), "Thank you. Your application is currently under review."))
-        actions.add_child(reply)
-        var req = _button("Request Documents", 160)
-        req.pressed.connect(func(): _request_documents_by_id(int(app.id)))
-        actions.add_child(req)
-        var counter = _button("Open Loan Terms", 150)
-        counter.pressed.connect(func(): selected_application = _active_application_index_by_id(int(app.id)); _show_view("Loan Desk"))
-        actions.add_child(counter)
-    var archive = _hud_button("Archive", 100)
-    var current_mid = int(message.id)
-    archive.pressed.connect(func(): _archive_mail(current_mid))
-    actions.add_child(archive)
-    return outer
+        var reply = Button.new(); reply.text = "↩  Reply"; reply.custom_minimum_size.x = 105; reply.pressed.connect(func(): _send_application_reply(int(app.id), "Thank you. Your application is currently under review.")); actions.add_child(reply)
+        var req = Button.new(); req.text = "Request documents"; req.pressed.connect(func(): _request_documents_by_id(int(app.id))); actions.add_child(req)
+        var terms = Button.new(); terms.text = "Open loan terms"; terms.pressed.connect(func(): selected_application = _acti